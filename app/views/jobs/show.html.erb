<h1> <%= @job.title %> </h1>

<p>
  <%= simple_format(@job.description) %>
  <%= simple_format(@job.wage_lower_bound) %>
  <%= simple_format(@job.wage_upper_bound) %>
  <%= simple_format(@job.contact_email) %>


</p>
